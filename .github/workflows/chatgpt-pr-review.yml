name: ChatGPT README PR Enhancer

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  enhance-readme:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install jq
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Read current README
        id: readme
        run: |
          if [ -f "README.md" ]; then
            echo "content<<EOF" >> $GITHUB_OUTPUT
            cat README.md >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
          else
            echo "content<<EOF" >> $GITHUB_OUTPUT
            echo "El proyecto aún no tiene README.md" >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
          fi

      - name: Generate enhanced README with ChatGPT (debug)
        id: chatgpt
        run: |
          README_CONTENT=$(echo "${{ steps.readme.outputs.content }}" | sed 's/"/\\"/g')

          echo "=== Enviando contenido del README a ChatGPT ==="

          RESPONSE=$(curl -s -w "\n%{http_code}" \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${{ secrets.OPENAI_API_KEY }}" \
            -d "{
              \"model\": \"gpt-4-turbo\",
              \"messages\": [
                {
                  \"role\": \"system\",
                  \"content\": \"Eres un redactor técnico experto en R y documentación. Mejora el README.md manteniendo el contenido actual, pero con tono profesional, estructura clara, ejemplos y sugerencias de imágenes. No inventes código ni borres información útil.\"
                },
                {
                  \"role\": \"user\",
                  \"content\": \"Aquí está el README actual:\n\n$README_CONTENT\"
                }
              ],
              \"temperature\": 0.4
            }" \
            https://api.openai.com/v1/chat/completions)

          BODY=$(echo "$RESPONSE" | head -n -1)
          STATUS=$(echo "$RESPONSE" | tail -n 1)

          echo "=== CÓDIGO DE ESTADO ==="
          echo "$STATUS"
          echo "=== CUERPO COMPLETO DE LA RESPUESTA ==="
          echo "$BODY"

          if [ "$STATUS" -ne 200 ]; then
            echo "⚠️ La API devolvió error $STATUS, manteniendo README original."
            cp README.md README_new.md
            exit 0
          fi

          OUTPUT=$(echo "$BODY" | jq -r '.choices[0].message.content // empty')

          echo "=== CONTENIDO EXTRAÍDO DE LA RESPUESTA ==="
          echo "$OUTPUT"

          if [ -z "$OUTPUT" ]; then
            echo "⚠️ ChatGPT no devolvió contenido válido (vacío o null)."
            cp README.md README_new.md
          else
            echo "$OUTPUT" > README_new.md
          fi

      - name: Check for differences
        id: diff
        run: |
          if ! diff -q README.md README_new.md > /dev/null; then
            echo "changed=true" >> $GITHUB_OUTPUT
          else
            echo "changed=false" >> $GITHUB_OUTPUT
          fi

      - name: Create Pull Request with updated README
        if: steps.diff.outputs.changed == 'true'
        uses: peter-evans/create-pull-request@v5
        with:
          commit-message: "📝 Mejora automática del README con ChatGPT"
          branch: chatgpt/readme-update
          title: "Mejora automática del README.md"
          body: |
            Este Pull Request ha sido generado automáticamente por ChatGPT.
            
            **Cambios detectados:**
            - Revisión de estilo y claridad.
            - Mejora del tono profesional y formato.
            - Sugerencias de secciones adicionales o ejemplos.

            Revisa el nuevo README.md antes de fusionarlo.
          add-paths: README_new.md

      - name: Rename file to README.md for PR
        if: steps.diff.outputs.changed == 'true'
        run: mv README_new.md README.md
